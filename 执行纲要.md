# VoxTell Vision-Language World Model：Codex 开发执行纲要

## 总体要求

这是一个独立的新项目，从零开发，不复用 DHGA 的网络、trainer、loss、router、expert 或其他设计。原始基础模型使用 VoxTell，目标是在 VoxTell 的 vision-language latent space 上建立一个轻量级 action-conditioned World Predictor，并最终用于 source-free domain adaptation。

整个开发必须按照 V0 → V1 → V2 → V3 → V4 → V5 的顺序进行。每个版本只增加一个主要能力。当前版本通过基本验证以后，再继续开发下一版本。不要一次实现完整系统。

第一阶段以“证明 world model 能否学习 VoxTell latent state transition”为核心。SFDA、EMA teacher、semantic anchor、counterfactual consistency loss 等均放到后续阶段。

voxtell目录：/data/zy/VoxTell_from_disk
数据目录：/data/zy/CT_MRI_DATA_3D/images/P0  /data/zy/CT_MRI_DATA_3D/labels/P0
训练集测试集分布：worst_zeroshot_split_p0

不要修改voxtell原始代码，所有 world model 相关的修改和新增都放在独立目录中。
---

## V0：建立基础工程和 Baseline

首先整理 VoxTell 的独立运行环境和模型接口，确保可以正常完成训练数据和目标域数据的推理。

需要支持从 VoxTell forward 中获取三类信息：

1. 最终 full-resolution segmentation prediction。
2. decoder 各个 scale 的中间 vision-language feature。
3. 每个 decoder scale 对应的已有 segmentation head 输出。

增加统一的 feature hook/interface，例如：

`forward_with_states(image, prompt)`

返回：

`final_prediction`

`decoder_states`

`intermediate_predictions`

暂时不修改 VoxTell 参数。

同时建立三个 baseline：

`B0 = original VoxTell`

`B1 = VoxTell + real multi-view augmentation`

`B2 = VoxTell + multi-prompt inference/consistency`

这几个 baseline 只用于后续比较，不需要进行复杂调参。

---

## V1：确定 World State，并实现最小 Visual World Model

VoxTell 全部冻结。

首先选择两个 decoder candidate scale，例如中间 decoder layer 和倒数第二个 decoder layer。

分别提取对应的 vision-language feature，作为候选 world state。

使用一个小规模 development subset，对两个候选层进行简单比较。比较内容只需要包括：

* feature 空间大小和显存消耗；
* 该层 segmentation head 的分割能力；
* World Predictor 对该层状态的预测难度。

最终选择一个计算成本较低，同时保留较强 segmentation information 的 decoder layer。

选定以后固定下来，后续版本不要频繁更换 world state。

### World Predictor

实现一个轻量级 3D residual predictor：

输入：

`current_state`

`action_embedding`

输出：

`predicted_next_state`

World Predictor 可以采用简单结构：

`channel projection → action conditioning → 2~3 residual 3D conv blocks → output projection + residual connection`

action conditioning 推荐使用 FiLM 或简单的 scale/bias modulation。

第一版只支持：

`gamma`

每个 action 包含：

`action_type`

`action_strength`

例如：

`gamma, +0.2`

### V1 数据生成

原始图像经过冻结 VoxTell：

`original image + prompt → state_original`

同一图像做 gamma：

`gamma image + same prompt → state_target`

World Predictor 输入：

`state_original + gamma_action`

输出：

`state_predicted`

训练目标：

使 `state_predicted` 接近 `state_target`。

V1 只使用一个 World Prediction Loss，可以使用 normalized MSE、Smooth-L1 或 cosine distance。先选择一种简单稳定实现，不叠加多个 loss。

### V1 必须完成的对照

实现三个实验：

`Identity`: 直接使用 original state 预测 target state。

`Action-agnostic`: Predictor 只输入 state，不输入 action。

`Action-conditioned`: Predictor 输入 state + action。

验证：

`Action-conditioned` 是否明显优于 `Action-agnostic`。

同时把 predicted state 接入 VoxTell 该 decoder scale 已有 segmentation head，比较 predicted state 对应 mask 与真实 gamma state 对应 mask 的差异。

V1 完成后暂停开发并输出实验结果。

---

## V2：扩展 Visual Action Space

保持 VoxTell 全部冻结，保持 V1 已通过的 world state：

`decoder_stage_1_low_to_high`

保持 World Predictor 当前结构不变：

* hidden_channels = 16；
* 2 个 residual 3D conv block；
* FiLM conditioning 不变；
* output projection zero-init；
* normalized MSE loss 不变，不增加新 loss。

增加第二种 visual action：

`contrast`

contrast augmentation 定义为：

`contrast_factor = 1 + strength`

以图像均值为中心：

`x_new = mean + contrast_factor * (x - mean)`

最后 clip 到输入 intensity range。V2.1 起，contrast 在 VoxTell preprocessing 之后、patch padding/slicing 之前执行；gamma 仍保持 V1/V2 的 raw-image augmentation 流程，即在 VoxTell preprocessing 之前执行。

V2 第一版 contrast strengths 使用：

`[-0.30, -0.15, +0.15, +0.30]`

V2 使用显式可区分的 action encoding：

`gamma(s) = [1, 0, s]`

`contrast(s) = [0, 1, s]`

因此 World Predictor 的 action_dim 改为 3；不要增加额外 action encoder 或复杂网络。

同一个 World Predictor 混合训练 gamma 和 contrast transition。训练数据应平衡包含两种 action type 和四种 strength，继续使用 V1.3 的 patch selection、shuffle、8 train / 4 validation、micro/macro/per-case/per-action evaluation 逻辑。

现在 World Predictor 需要区分：

`gamma + strength`

`contrast + strength`

验证同一个 World Predictor 是否能够根据不同 action 预测不同的 target state。

继续保留：

`Action-agnostic`

vs

`Action-conditioned`

对照。

增加两种 evaluation-only wrong-action：

* wrong-strength：保持 action type 正确，但 strength 取反；
* wrong-type：保持 strength 不变，但交换 action type。

wrong-action 只用于 evaluation，不增加训练 loss。

V2 输出至少包括：

* overall micro state / mask-logit error；
* overall macro state / mask-logit error；
* gamma action family 单独结果；
* contrast action family 单独结果；
* per-strength 结果；
* per-case 结果；
* correct vs wrong-strength；
* correct vs wrong-type；
* case win rate；
* action-family win result。

V2 判断标准：

* unseen overall micro 和 macro 中 Action-conditioned < Action-agnostic；
* correct action 明显优于 wrong-strength；
* correct action 明显优于 wrong-type；
* gamma 和 contrast 两个 action family 分别看时，Action-conditioned 都总体优于 Action-agnostic；
* 不要求每个 case、每个 strength 100% 通过，多数稳定通过即可。

当前阶段不要加入 rotation、flip 等明显影响解剖空间关系的增强。

如果 gamma + contrast 的 action-conditioned state prediction成立，进入 V3。

---

## V3：加入 VLM 特有的 Language Action

World Predictor 网络主体保持不变。

增加 language action encoder。

使用 VoxTell 原始 text encoder 得到：

`text_embedding_original`

`text_embedding_target`

构造 language action：

`language_action = projection(text_embedding_target - text_embedding_original)`

第一阶段 language action 使用 semantic-preserving prompt：

例如：

`liver → hepatic organ`

`kidney → renal organ`

`left ventricle → LV`

同一张图像保持不变。

真实 target state：

`same image + target prompt → VoxTell → state_target`

World Predictor：

`state_original + language_action → state_predicted`

继续只使用 World Prediction Loss。

验证 World Predictor 是否能够根据 language action 预测 prompt 改写后的 vision-language state。

同时比较：

`multi-prompt consistency baseline`

与：

`language-action world prediction`

---

## V4：加入 Semantic-changing Language Action

保持 V3 的网络和训练方式。

加入会真正改变 segmentation target 的语言变化，例如：

`left kidney → right kidney`

`left lung → right lung`

输入图像保持一致。

原始 state 表示原始 prompt 对应的 anatomical grounding。

target state 由目标 prompt 经过冻结 VoxTell 得到。

训练 World Predictor 根据 language action 预测新的 grounding state。

该阶段重点验证：

World Predictor 能否根据语言语义变化，把 latent state 从一个 anatomical target 转移到另一个 anatomical target。

这个实验用于证明模型能够学习 language-conditioned state transition。

此类 semantic-changing language action 暂时只用于 world-model capability evaluation，不参与后续 voxel reliability variance 计算。

---

## V5：实现 Visual-Language Multi-step Rollout

保持同一个 World Predictor，不增加新的网络模块。

实现连续调用：

`state0`

→ `visual action`

→ `state1_pred`

→ `language action`

→ `state2_pred`

例如：

`original image + liver`

先执行：

`gamma +0.2`

再执行：

`liver → hepatic organ`

真实 endpoint 通过：

`gamma image + hepatic organ → frozen VoxTell`

获得。

比较：

`rollout predicted endpoint`

与：

`real VoxTell endpoint`

首先测试：

`gamma + paraphrase`

然后：

`contrast + paraphrase`

如果前两种稳定，再测试：

`visual action + semantic-changing language action`

V5 主要验证 World Predictor 是否具有连续 visual-language state transition 能力。

---

## V6：World-guided Reliability for SFDA

V1-V5 验证完成以后，再开始 source-free adaptation。

目标图像首先只进行一次完整 VoxTell forward：

得到：

`final full-resolution pseudo-label`

以及：

`selected decoder world state`

从同一个 state 出发，让 World Predictor生成少量 imagined worlds。

建议第一版 K=3，例如：

`gamma`

`contrast`

`semantic-preserving prompt change`

每个 predicted state 经过选定 decoder scale 原有的 segmentation head，得到低分辨率 probability map。

在这些 semantic-preserving imagined worlds 之间计算 voxel-wise disagreement / variance。

得到：

`low-resolution world reliability map`

然后使用 trilinear interpolation 上采样到 full-resolution pseudo-label 尺寸。

最终 pseudo-label weight 同时考虑：

`full-resolution VoxTell confidence`

和：

`upsampled world reliability`

第一版只实现简单稳定的组合方式。

World Predictor 此时首先负责 reliability estimation。

不要加入额外 counterfactual consistency training loss。

---

## V7：使用 World Reliability 进行 VoxTell Adaptation

建立最简单的 pseudo-label SFDA trainer。

teacher 首先使用 frozen VoxTell。

pseudo-label 来自 VoxTell full-resolution prediction。

每个 voxel 的训练权重由 V6 的 world reliability 和原始 prediction confidence 决定。

student 第一版只更新 VoxTell 中少量与 decoder / vision-language fusion 相关的参数。

不要直接更新整个 encoder。

建立以下消融：

`A0 = pseudo-label SFDA`

`A1 = pseudo-label + real multi-view reliability`

`A2 = pseudo-label + World Predictor imagined reliability`

重点比较：

segmentation performance；

pseudo-label accuracy；

运行时间；

GPU memory；

一次 target volume 所需要的完整 VoxTell forward 次数。

这里需要体现 World Model 的效率优势：

`real multi-view = K 次完整 VoxTell forward`

`world model = 1 次完整 VoxTell forward + K 次 lightweight World Predictor`

---

## V8：后续可选消融，不提前实现

只有 V7 成功以后再依次评估以下组件。

### Optional A：Counterfactual Training Loss

利用 World Predictor predicted states 直接约束 student VoxTell 的 target representation。

作为独立消融加入：

`World reliability`

vs

`World reliability + counterfactual consistency loss`

先验证是否真正提升，再决定是否保留。

### Optional B：EMA Teacher

如果 frozen teacher 成为明显性能瓶颈，再增加 EMA teacher。

### Optional C：Semantic Anchor

如果 SFDA 后发现 prompt robustness 或 language grounding 明显下降，再增加 semantic preservation loss。

### Optional D：Spatial Actions

最后测试：

small translation；

small scaling；

small crop；

small rotation。

rotation 单独测试不同角度，不默认加入主方法。

---

## 工程组织要求

建议建立独立目录，例如：

`world_voxtell/`

其中至少包含：

`models/world_predictor.py`

`models/action_encoder.py`

`models/voxtell_state_interface.py`

`data/actions.py`

`training/train_world_model.py`

`training/train_sfda.py`

`evaluation/eval_world_prediction.py`

`evaluation/eval_rollout.py`

`evaluation/eval_sfda.py`

`configs/`

`utils/`

World Model 与 VoxTell 的接口保持模块化。不要直接大规模修改 VoxTell 原始代码。

所有版本通过 config 控制，避免复制多个 trainer。

例如：

`world.visual_actions`

`world.language_actions`

`world.enable_rollout`

`sfda.enable_world_reliability`

`sfda.enable_counterfactual_loss`

后续消融通过开关完成。

---

## Codex 每阶段执行规则

每次只执行当前指定版本。

完成后必须输出：

1. 修改和新增的文件；
2. 当前模型 forward 数据流；
3. 当前训练参数中哪些被冻结、哪些可训练；
4. 当前 loss；
5. action 的定义和采样方式；
6. smoke-test 命令；
7. smoke-test 输出；
8. 当前版本尚未实现的下一阶段功能。

不要提前实现后续版本。

不要因为代码接口方便而自动加入 EMA、semantic anchor、额外 uncertainty network、multi-scale world predictor 或新的 segmentation head。

优先复用 VoxTell 已有 decoder segmentation head。

整个开发遵循：

**一个问题 → 一个新增能力 → 一次实验验证 → 决定是否进入下一阶段。**
